# 登攀 / TOZANBU

ランダム生成された山を 1〜複数人で登り、山頂を目指す 3D 登山ゲーム。
未踏峰を最初に登頂したパーティーには命名権が与えられ、その名前と初登頂記録が全プレイヤーに共有される。

Three.js + TypeScript + Vite / マルチプレイと永続化は Cloudflare Workers (Durable Objects)。

## 起動

```bash
npm install
```

ソロで遊ぶだけならこれだけでよい。

```bash
npm run dev
```

マルチプレイと山の記録のサーバを使う場合は、別のターミナルで:

```bash
npm run dev:mp
```

`npm run dev` は `/api` と `/ws` を `127.0.0.1:8787` へプロキシする。
サーバを起動していない場合、山の記録は自動的に localStorage へフォールバックし、ソロで問題なく遊べる。

その他のコマンド。

| コマンド | 内容 |
| --- | --- |
| `npm run typecheck` | クライアントと Worker の型検査 |
| `npm run test:mp` | wrangler dev を立ててマルチプレイと記録 API を検証 |
| `npm run build` | 型検査 + 本番ビルド (`dist/`) |
| `npm run deploy` | ビルドして Cloudflare へ配信 |

## 操作

操作キーは増やさない。地形との関係で同じ入力の意味が変わる。

| 入力 | 通常時 | ルート組み立て中 | 登攀の実行中 |
| --- | --- | --- | --- |
| `W` `A` `S` `D` | 移動 | — | `S` で手を放す |
| `Shift` | ダッシュ | ロープを使うか切り替え | — |
| `Space` | ジャンプ／段差ではマントリング | **組み立てたルートを登る** | — |
| マウス移動 | 視点（ポインタロック） | 岩肌のセルを指す | — |
| 左クリック | 視点をロック | **ルートに1手足す** | — |
| 右クリック | — | 1手戻す | — |
| 左ドラッグ | — | 視点を回す | — |
| ホイール | — | 寄る／引く | — |

- 岩壁に正面から `W` を押し当て続けると、専用キーなしで登攀へ移行する。
- 通常時は画面中央のレティクル、ルート組み立て中はカーソルで狙う。
  軌道カメラは注視点の近くを画面中央に持ってこられず、
  手の届く範囲のセルはレティクルでは原理的に狙えないため。
- ポインタロックが許可されない環境（埋め込み表示など）では、通常時もドラッグで視点を回す。

## 7つの基本アクション

歩行・ダッシュ・ジャンプ・マントリング・登攀・トラバース・落下。
滑り／しゃがみ／匍匐／壁走り／バランス操作は採用していない。

- **マントリング**: ジャンプでは越えられない 0.75〜2.7m の段差の前で `Space`。縁を掴んで引き上げる。
  段差の上が棚になっている（先まで平らで歩ける）ときだけ成立するので、岩壁の途中では使えない。
- **トラバース**: 登攀中の横移動。移動量の水平成分が大きいと自動でトラバース表示になる。
- **落下**: 崖から踏み外す／スタミナが尽きる／`S` で手を放す。

### 斜度の3段階

| 斜度 | できること |
| --- | --- |
| 〜38度 | 歩いて登れる |
| 38〜54度 | **登って越えるしかない**。立って横切る・下ることはできる |
| 54度〜 | 立っていられない。ジャンプで飛び込んでも掴まれずに滑り落ちる |

歩けない斜面は必ず岩壁として登攀できる（同じ「進行方向の上昇率」で判定しているので、
「歩けないのに登れもしない」詰み斜面ができない）。判定はハイトフィールドの中心差分ではなく
進行方向 1.2m 先までの実際の上昇量で行う。テラスの縁で誤って足止めされないため。

## スタミナ

| | 内容 |
| --- | --- |
| 現在スタミナ | 短期的な運動能力。ダッシュ・登攀・ラッセルで減り、休憩で最大まで戻る |
| 最大スタミナ | 登山全体を通した疲労・身体状態。時間と環境で減っていく |

酸素・体温・疲労に独立したゲージは作らず、
**最大スタミナの減少量**と**現在スタミナの回復速度**への補正として表現する。

- 標高が上がると酸素が薄くなり、回復が鈍り疲労が早まる（`ENV.altitudeStart` 以上）
- 気温は 1000m あたり 6.5℃ 下がる。氷点下に近づくほど消耗が増える
- 運動強度（ダッシュ・ラッセル・登攀）が高いほど最大スタミナが削れる
- 岩棚（ledge）の上と地上での休憩で現在スタミナが回復する

## 登攀

このゲームの主要なルート選択要素。**岩肌を見て、登頂可能なルートを見つける**。

### 内部はグリッド、見た目は連続した岩壁

岩壁は内部的に 2D グリッド（1セル 1.25m）へ分割し、各セルに登攀難易度を持たせる。
**グリッドそのものはプレイヤーに見せない。** 難易度は岩肌の造形で伝える。

| 難易度 | 岩肌の見た目 | 基礎消費 |
| --- | --- | --- |
| Easy | 大きな突起、岩棚 | 4 |
| Medium | 小さな突起、凹凸 | 9 |
| Hard | 細いクラック、小さなエッジ | 19 |
| Impossible | ほぼ平滑な岩壁（何も無い） | 通れない |

```
消費 = セル難易度 × 移動方向 + 移動距離
移動方向: 登り ×1.0 ／ 横 ×0.82 ／ 下り ×0.58
```

難易度の割り当ては「登りやすさ」の連続場（縦に伸びる筋 + 横に走る帯）の**順位**で決めるので、
狙った比率をそのまま出しつつ、クラックや岩棚が線としてつながって見える。

手足や個々のホールド位置はゲームロジックでは扱わない。必要ならアニメーション側の話にする。

### 「見て決める」と「実行する」を分ける

1. 岩壁に `W` を押し当てると、壁全体が入る位置まで下がって見上げる視点になる
2. 岩肌を観察し、通るセルを順にクリックしてルートを組み立てる
3. `Space` で実行。指定したルートを自動で登る
4. 登り切れば抜けられる。**読み違えて力尽きれば落ちる**

組み立て中に出る情報は次の3つだけ。

| | |
| --- | --- |
| そこへ手が届くか | 届くセルだけカーソルが乗る |
| そのセルの消費と残り | `消費 12 → 残り 64（岩棚）` |
| ルート全体の見込み | 手数・総消費・登り切った時の残り・終点の種類 |

残りが足りなくなる手があれば「**5手目で力尽きて落ちる**」と先に出る。
それを承知で登るかどうかがプレイヤーの判断になる。

- **正解ルートは表示しない**。示すのは「届くか」と「いくら掛かるか」だけ
- 最短ルートが最適とは限らない。短いが難しい直登、遠回りだが簡単な線、途中で休憩できる線が同じ壁に同居する

### 岩棚はルートを組み直せる場所

ルートの終点は3種類あり、実行後の扱いが変わる。

| 終点 | 結果 |
| --- | --- |
| 壁の上へ抜けられるセル | マントリングで抜けて歩行へ戻る |
| 岩棚 | スタミナが戻り、**そこからもう一度ルートを組み立てる**（マルチピッチ） |
| それ以外 | 掴まる場所が無く落ちる |

### 生成と保証

1. セルごとに壁面の座標を求める（取れないセルは通行不可）
2. 難易度を順位で割り当て、Easy の一部を岩棚にする
3. 「地上から取り付けるセル → 壁の上へ抜けられるセル」の突破可能ルートが
   スタミナ 100 以内で存在するかをグリッド経路探索で検査する（岩棚に着いたら消費はリセット）
4. 存在しなければ**岩肌を補修する**（途切れ目の岩を1段易しくする／区間の中央を岩棚にする）

最大 50 回繰り返すので、**壁には必ず最低1本の突破可能ルートがある**。

#### 検証結果（各20枚の岩壁を生成）

| | 難易度3 | 難易度5 |
| --- | --- | --- |
| 突破可能ルートの保証 | 20/20 | 20/20 |
| 難易度分布（易/並/難/不可） | 34/34/27/5% | 20/27/37/16% |
| 平均手数 | 7 | 14 |
| 平均総消費 | 50 | 124 |
| 岩棚から岩棚までの最大消費 | 30 | 63 |
| 生成時間 | 2.2ms/枚 | 2.3ms/枚 |

### 積雪・ラッセル

積雪量は標高・傾斜・風下・地形の凹凸で変わる。専用操作はない。

- 積雪 18cm 以上を歩くと自動的にラッセルになる。移動が遅くなり、深さに応じてスタミナを消費する
- 未踏の深雪では**進行方向の雪**で判定するので、先頭は自分の踏み跡で楽をできない
- 通過した場所は「踏み跡」として残り、後続は消費が大幅に減る（実測: 先頭 8.2 → 後続 3.7）

登攀のロープと同じ原則:

```
ラッセル: 先頭が高コスト → 踏み跡 → 後続が低コスト
登攀:     先頭が高コスト → ロープ   → 後続が低コスト
```

これをマルチプレイにおける協力の中心システムとしている。

## 山の生成

Seed と難易度から決定論的に生成する。同じ Mountain ID なら誰がプレイしても同じ地形になる。

```
Mountain ID = M<seedの36進数>-<難易度>     例: M000039U-3
```

生成要素:

- **山体** ほぼ直線的な円錐（歩ける斜度に収まるようにしてある）
- **尾根 / 谷** リッジノイズと fBm を中腹で強く効かせる
- **崖バンド** 標高を段状にリマップして「垂直に近い壁 + その上のテラス」を作る。
  バンドの立ち上がり幅は場所によって変わるので、同じバンドでも
  **歩いて抜けられる弱点**と**登るしかない壁**ができる（＝マクロなルートファインディング）
- **岩質** 花崗岩／安山岩／砂岩／石灰岩。セル難易度の出やすさと岩屑の量に効く
- **表面** 基盤岩 → 岩屑・礫 → 土壌 → 植生 → 積雪 の順に重ねる。
  標高だけでなく傾斜・岩質・侵食・湿りから決める
- **植生** 森林限界以下に樹木を配置（カメラ手前はディザで間引いて視界を確保）

### 難易度

数値の強さではなく、山そのものが難しくなる。

| | 内容 | 山頂 | 歩きだけで登頂 | 歩きだけの到達高度 | 要登攀 |
| --- | --- | --- | --- | --- | --- |
| 1 ハイキング | 歩行中心。ときどき小さな段差 | 108m | できる | 100% | 2 |
| 2 一般登山 | 急斜面と岩場。短い登攀と浅い雪 | 150m | 山による | 95% | 17 |
| 3 岩稜 | 長い登攀と積雪。ルートファインディングが要る | 181m | **できない** | 57% | 28 |
| 4 高難度 | 深雪と連続する岩壁 | 208m | **できない** | 12% | 21 |
| 5 最高難度 | どのルートを選ぶかが登頂可否を決める | 233m | **できない** | 2% | 18 |

Seed 5種 × 難易度5段階（計25山）で計測した値。**25山すべてで登頂可能**。

難易度3以上は、どう歩き回っても歩行だけでは山頂に届かない。
最高難度では標高の 2% までしか歩けず、最初から登攀になる。

崖バンドの「弱点」の位置は標高によってずれる。
(x, z) だけで決めると段化しないセクターが山麓から山頂まで一直線のスロープになり、
そこを歩くだけで登れてしまうため、標高でもノイズをずらしている。

生成した山は起動時に [reachability.ts](src/mountain/reachability.ts) で2通り検査する。

- **登攀あり**: 登山口から山頂へ到達できるか（できなければ警告）
- **歩行のみ**: 歩行とマントリングだけで登頂できてしまわないか（難易度3以上でできてしまえば警告）

## マルチプレイ

協力マルチプレイが正式な仕様。シングルプレイは 1 人パーティーとして同じルールで動く。

タイトルでルーム名を入れると、**同じ山 + 同じルーム名**のプレイヤーと同じ部屋に入る。

サーバ（Durable Object）がゲーム状態の正となり、次を同期する。

- プレイヤーの位置・向き・状態
- 現在／最大スタミナ
- ロープ
- 踏み跡
- 登頂記録

静的な山岳地形は同期しない。Seed から各クライアントが生成する。

## 山の永続性

サーバには次を保存する。

```
Mountain ID / Seed / 難易度 / 山名 / 初登頂者 / 初登頂パーティー / 初登頂日時 / 登頂者数
```

- 未踏峰には正式名称がなく、`未踏峰 M000039U-3` と表示される
- 初登頂が成立すると、そのプレイヤーまたはパーティーに命名権が与えられる
- 一度命名された山は、以降すべてのプレイヤーに同じ名前で表示される（改名はできない）

## 実装構造

```
src/
├── main.ts                       ゲーム本体 (シーン・ループ・各システムの結線)
├── core/
│   ├── math.ts                   clamp / lerp / damp / smoothstep
│   ├── rng.ts                    決定論的な乱数と勾配ノイズ (fBm / リッジ)
│   ├── types.ts                  共有型と調整値 (CLIMB / MOVE / STAMINA / ENV)
│   ├── Input.ts                  WASD / Shift / Space / マウス (ポインタロック)
│   └── CameraRig.ts              三人称カメラ + 地形と樹木への当たり
├── mountain/
│   ├── difficulty.ts             難易度プロファイル
│   ├── Heightfield.ts            Seed からのハイトフィールド生成と問い合わせ
│   ├── SurfaceMap.ts             表面分類 (岩・岩屑・土壌・植生・積雪) と岩質
│   ├── MountainMesh.ts           地形メッシュ・樹木・転石・踏み跡テクスチャ
│   ├── Mountain.ts               山のファサードと MountainRecord
│   ├── MountainRegistry.ts       記録のサーバ同期 (失敗時 localStorage)
│   ├── reachability.ts           山頂到達可能性の検査
│   └── cliff/
│       ├── grid.ts               セルの型・難易度・移動コスト
│       ├── ClimbWall.ts          岩壁1枚のグリッド生成・岩肌の造形・ルート保証
│       ├── WallFinder.ts         正面の岩壁の検出とキャッシュ (決定論的な壁ID)
│       └── routeSolver.ts        グリッド経路探索 (岩棚で区間リセット)
├── player/
│   ├── PlayerController.ts       歩行 / ダッシュ / ジャンプ / マントリング / 落下 / ラッセル
│   └── ClimbingController.ts     登攀 / トラバース / ロープ / トップアウト
├── systems/
│   ├── StaminaSystem.ts          現在・最大スタミナと環境補正
│   ├── SnowSystem.ts             積雪・ラッセル・踏み跡グリッド
│   └── RopeSystem.ts             有限のロープと固定
├── net/
│   ├── NetClient.ts              WebSocket クライアント
│   └── RemotePlayers.ts          他プレイヤーの表示と補間
└── ui/
    ├── HUD.ts                    状態・スタミナ・山・パーティー・ヒント
    └── TitleScreen.ts            山選択 (Seed / 難易度 / 記録一覧)

worker/
├── index.ts                      ルーティング (静的 / /ws / /api)
├── MountainRoom.ts               パーティー部屋 (位置・ロープ・踏み跡・登頂)
├── MountainRegistry.ts           山の記録 (初登頂・命名・登頂者数)
└── types.ts
```

## 仕様からの相違点

**登攀中の上下移動を WASD に割り当てていない。**
仕様には「WASD: 移動／登攀中の移動」とあるが、方向キーでセルを自動選択すると
「候補の中から選ぶ」が「その方向に進む」に変わってしまい、
`W` を押しっぱなしにするだけでルートを登り切れてしまう。
ルートを見比べて選ぶ行為自体がこのゲームの中心なので、
登攀中のルート指定は**視点＋クリック**（＝仕様の「マウス: 登攀ルート選択」）に一本化した。
`S` は「手を放す」に割り当ててあり、操作キーは増えていない。

## 今回入れていないもの

リアルな物理挙動、手足の IK、クライミングアニメーション、ロープの物理シミュレーション、
天候変化、アイテム、時間経過による日照変化。
